clear
close all
% ча�?тота ди�?кретизации по времени
dt = 1;

% начaльна�? точка
x0 = 50;
%y0 = 20;
y0 = 0;
t0 = 0; % начальное врем�?

% �?коро�?ть движени�? первого �?амолета
vx_plane = 0.1;
%vx_plane = 0;
vy_plane = 0;
%vy_plane = 0;


% начальна�? �?коро�?ть второго
vx =  0.8;
vy =  0.1;

% углова�? �?коро�?ть второго
gradus = 10;
w = 2 * pi * gradus / 360;

psi_in = 5; 

t1 = 260; % конечное врем�?

[x, y, t, vvx, vvy, N, xc, yc, psi_out] = standartTurnFromUniformPlane(t0, t1, dt, vx_plane, vy_plane, x0, y0, vx, vy, w, psi_in);



figure(1)
hold on
for k = 1:N
% % %     plot(x(k),y(k), '*') % координаты второго ВС в �?и�?теме координат первого
% % %     plot(xc(k),yc(k), 'ro') % координаты центра поворота второго ВС в �?и�?теме координат первого
    plot(y(k),x(k), '*') % координаты второго ВС в �?и�?теме координат первого
    plot(yc(k),xc(k), 'ro') % координаты центра поворота второго ВС в �?и�?теме координат первого
    pause(0.001)
end
grid on
hold off
